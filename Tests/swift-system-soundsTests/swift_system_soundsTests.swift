import XCTest
@testable import swift_system_sounds

final class swift_system_soundsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_system_sounds().text, "Hello, World!")
    }
}
